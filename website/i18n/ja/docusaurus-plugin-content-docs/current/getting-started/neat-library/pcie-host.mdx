---
title: "PCIeホストをインストールします。"
description: "接続された Modalix PCIe カード上でモデルを実行するために、PCIe ホスト ランタイムをインストールしてください。"
sidebar_position: 3
---

このページを使用して、LinuxマシンにインストールされたModalix PCIeカードに接続された、Neat PCIeホストパッケージをインストールします。

Neat PCIeホストパッケージは、ホストマシンから接続されたModalix PCIeカードにデータを送信し、推論結果を受信するコプロセッシングAPIを提供します。

:::warning PCIeホストマシンにインストールします。
このインストールを、Modalix PCIeカードに接続された**PCIeホストマシン**で実行してください。Neat SDKコンテナ内、DevKit上、またはModalix PCIeカード上で実行しないでください。
:::

## サポート対象のホストオペレーティングシステム

Neat PCIe ホストパッケージは、以下の検証済みのホストプラットフォームをサポートします。

| オペレーティングシステム | アーキテクチャ | サポート状況 |
|---|---|---|
| Ubuntu 22.04 | x86_64 | サポート対象 |
| Ubuntu 24.04 | x86_64 | サポート対象 |

Arm64ホストでのビルドは、まだ検証されていません。Windows 11はサポート対象外です。

## 前提条件

パッケージをインストールする前に：

- PCIeホストに`sima-cli`をインストールします。[sima-cli のインストールガイド](/tools/sima-cli/)を参照してください。
- [PCIeドライバーのインストール方法](https://developer.sima.ai/hardware/getting-started/pcie-mode/driver-installation)に従って、PCIeホストドライバーをインストールし、検証します。
- Modalix PCIeカードを接続し、設定して、ホストから管理インターフェースにアクセスできるようにします。カード0のデフォルトアドレスは`10.0.0.2`です。
- カードに互換性のあるNeat Libraryバージョンをインストールします。ホストとカードのパッケージは、互換性のあるリリースから提供される必要があります。

PCIeホストを認証し、SiMa.aiのアーティファクトをダウンロードできるようにします。

<ShellCommand prompt="pcie-host">
sima-cli login
</ShellCommand>

## PCIeホストパッケージをインストールします。

PCIeホスト上で作業ディレクトリを選択し、インストールされたUbuntuのバージョンに対応するコマンドを実行してください。

**x86_64 アーキテクチャにおける Ubuntu 22.04:**

<ShellCommand prompt="pcie-host">
sima-cli neat install core/pciehost/ubuntu22/amd64@v0.4.0
</ShellCommand>

**x86_64 アーキテクチャにおける Ubuntu 24.04:**

<ShellCommand prompt="pcie-host">
sima-cli neat install core/pciehost/ubuntu24/amd64@v0.4.0
</ShellCommand>

インストーラーは、C++ APIや、`~/pyneatpcie` にあるPython環境など、Neat PCIeホストアプリケーションを開発および実行するために必要なすべてのものを追加します。

最初のインストール時、SSH プロビジョニングでは、カードの `sima` アカウントのパスワードを要求できます。これにより、`~/.ssh/sima_neat_pcie_ed25519` が作成または再利用され、その公開鍵がカードにインストールされます。その後、通常の PCIe 動作では、パスワードなしの SSH を使用して、カード側のモデル設定を行います。

## インストールを確認する

Python APIとPCIeホストプラグインが利用可能であることを確認してください。

<ShellCommand prompt="pcie-host">
source ~/pyneatpcie/bin/activate
python3 -c "import pyneatpcie; print('pyneatpcie ready')"
gst-inspect-1.0 neatpciehost
</ShellCommand>

## トラブルシューティング

SSHによるプロビジョニングでカードを自動的に検出できない場合は、まず、ホストが管理アドレスにアクセスできることを確認してください。

<ShellCommand prompt="pcie-host">
ping -c 1 10.0.0.2
</ShellCommand>

次に、インストールされたセットアップユーティリティを、指定されたアドレスで実行します。

<ShellCommand prompt="pcie-host">
pcie-setup.sh --hosts "10.0.0.2"
</ShellCommand>

パッケージのインストールまたはランタイムの起動に失敗した場合は、PCIeホストパッケージとカードにインストールされているNeat Libraryが、互換性のあるバージョンから提供されていることを確認してください。

## 次のステップ

ホストからカードへの完全な開発パスを検証するために、[PCIeによる共同処理](/develop-apps/hello-neat/pcie/) を続行してください。
