#include "PipelineSegmentRuntime.h"

namespace simaai::neat::runtime {
// PipelineSegmentRuntime is a state split in Phase 3B. Segment behavior remains
// in Run/RunCore-facing functions until the next internal unification slice.
} // namespace simaai::neat::runtime
