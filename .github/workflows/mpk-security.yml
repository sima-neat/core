name: MPK Security

on:
  push:
    branches:
      - "release/**"
    tags:
      - "v*"
  workflow_dispatch: {}

jobs:
  mpk-security-gate:
    name: mpk-security-gate
    if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'release/') }}
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: ./build.sh --install-deps-only

      - name: Install clang toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y clang

      - name: Run MPK security gate
        env:
          CC: clang
          CXX: clang++
        run: bash scripts/ci/run_mpk_security_gate.sh
