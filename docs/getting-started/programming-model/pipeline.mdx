---
title: Pipeline
description: Compose deterministic node chains and execute via Run
sidebar_position: 5
---

# Pipeline

In SiMa NEAT, a pipeline is an ordered composition of nodes and node groups assembled through [`Session`](/reference/{lsa}/classes/simaai-neat-session).

The result is deterministic graph wiring and a reproducible gst-launch representation.

```cpp
#include "neat/session.h"
#include "neat/nodes.h"
#include "neat/node_groups.h"
```

## Building blocks

- **Node**: atomic stage unit (decode, convert, preprocess, sink, etc.).
- **NodeGroup**: reusable ordered set of nodes (for example model stages or input groups).
- **Session**: composition boundary and runtime build/validate point.

Reference:
- [`PipelineRun`](/reference/{lsa}/classes/simaai-neat-pipelinerun)
- [`PipelineRunOptions`](/reference/{lsa}/structs/simaai-neat-pipelinerunoptions)
- [`Node` API](/reference/{lsa}/files/include-builder-node-h)
- [`nodes` namespace](/reference/{lsa}/namespaces/simaai-neat-nodes)

## Deterministic naming

Element names are generated deterministically from node order. This gives:

- Stable `describe()` output.
- Stable `describe_backend()` strings for reproduction/debug.
- Consistent diagnostics and probe attachment points.

## Execution options

- **Sync execution**: `push_and_pull(...)` or `run(...)` for simple request-response behavior.
- **Async execution**: `push(...)` / `pull(...)` with queue tuning and drop policy.

## Caps and negotiation (simple mental model)

SiMa NEAT relies on native GStreamer negotiation, but controls key boundaries:

- **Push pipelines (`Input`)**:
  caps are derived from actual input at build/start time, then enforced by runtime policy.
- **Source pipelines (file/RTSP/image groups)**:
  caps are negotiated by source/decode elements, optionally constrained with explicit caps nodes.
- **Output normalization**:
  `add_output_tensor(...)` inserts convert/scale/caps + sink to keep output contracts predictable.

For push pipelines, format/shape changes are handled automatically. Runtime presets and queue policies control latency/safety tradeoffs, and advanced memory limits can be set with `RunAdvancedOptions::max_input_bytes`.

Reference:
<<<<<<<< HEAD:docs/getting-started/programming-model/pipeline.mdx
- [`InputAppSrc`](/reference/{lsa}/classes/simaai-neat-inputappsrc)
- [`InputAppSrcOptions`](/reference/{lsa}/structs/simaai-neat-inputappsrcoptions)
- [`PipelineRunOptions`](/reference/{lsa}/structs/simaai-neat-pipelinerunoptions)
- [`OutputTensorOptions`](/reference/{lsa}/structs/simaai-neat-outputtensoroptions)
========
- [`Input`](/reference/api/classes/simaai-neat-inputappsrc)
- [`InputOptions`](/reference/api/structs/simaai-neat-inputappsrcoptions)
- [`RunOptions`](/reference/api/structs/simaai-neat-runoptions)
- [`OutputTensorOptions`](/reference/api/structs/simaai-neat-outputtensoroptions)
>>>>>>>> origin/main:docs/getting-started/programming-model/pipeline.md

## Why this matters

- Stable element naming for debugging and diagnostics.
- Reproducible `describe_backend()` for troubleshooting with native GStreamer tools.
- Composable model + media pipelines in one API surface.
- Preset-driven runtime behavior for caps transitions and buffer safety.

## See also

- [Inference Workflow](overview)
- [Node](node)
- [Graph](graph)
- [Session](session)
- [Input and Output](io)

## Tutorials

- [tutorial_0003_nodes_and_groups](../../tutorials/tutorial_0003_nodes_and_groups)
- [tutorial_0007_caps_negotiation](../../tutorials/tutorial_0007_caps_negotiation)
- [tutorial_0022_inputappsrc_advanced_caps](../../tutorials/tutorial_0022_inputappsrc_advanced_caps)
- [tutorial_0023_builder_graph_and_contracts](../../tutorials/tutorial_0023_builder_graph_and_contracts)
- [tutorial_0024_hybrid_graph](../../tutorials/tutorial_0024_hybrid_graph)
